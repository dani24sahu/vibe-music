# Upstream music API used only by the Next.js server adapter.
# The browser never calls this URL directly.
SAAVN_API_BASE_URL=
SAAVN_API_TIMEOUT_MS=

# Synced lyrics via LRCLIB. No API key. Browser never calls this URL directly.
LRCLIB_API_BASE_URL=
LRCLIB_API_TIMEOUT_MS=
